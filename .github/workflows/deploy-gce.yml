name: Deploy to Google Compute Engine

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2


      - name: Build Docker image
        run: |
          docker build -t us-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/tiktok-backend/tiktok-backend:$GITHUB_SHA .

      - name: Push Docker image
        run: |
          gcloud auth configure-docker us-docker.pkg.dev
          docker push us-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/tiktok-backend/tiktok-backend:$GITHUB_SHA

      - name: Deploy to GCE VM
        run: |
          gcloud compute ssh ${{ secrets.GCE_INSTANCE_NAME }} --zone=${{ secrets.GCE_ZONE }} --command 'echo "${{ secrets.ENV_FILE }}" > /home/ubuntu/tiktok.env && chmod 600 /home/ubuntu/tiktok.env'
          gcloud compute ssh ${{ secrets.GCE_INSTANCE_NAME }} --zone=${{ secrets.GCE_ZONE }} --command "\
            docker pull us-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/tiktok-backend/tiktok-backend:$GITHUB_SHA && \
            docker stop tiktok-backend || true && \
            docker rm tiktok-backend || true && \
            docker run -d --name tiktok-backend -p 80:5000 --env-file /home/ubuntu/tiktok.env us-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/tiktok-backend/tiktok-backend:$GITHUB_SHA\
          "
